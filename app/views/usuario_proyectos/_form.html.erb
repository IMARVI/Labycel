<%= form_for(usuario_proyecto) do |f| %>
  <% if usuario_proyecto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(usuario_proyecto.errors.count, "error") %> prohibited this usuario_proyecto from being saved:</h2>

      <ul>
      <% usuario_proyecto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :usuario_id %>
    <%= f.number_field :usuario_id %>
  </div>

  <div class="field">
    <%= f.label :proyecto_id %>
    <%= f.number_field :proyecto_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
