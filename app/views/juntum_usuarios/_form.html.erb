<%= form_for(juntum_usuario) do |f| %>
  <% if juntum_usuario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(juntum_usuario.errors.count, "error") %> prohibited this juntum_usuario from being saved:</h2>

      <ul>
      <% juntum_usuario.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
