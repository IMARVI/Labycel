<%= form_for(proyecto) do |f| %>
  <% if proyecto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(proyecto.errors.count, "error") %> prohibited this proyecto from being saved:</h2>

      <ul>
      <% proyecto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="field">
    <%= f.label :fecha_inicio %>
    <%= f.datetime_select :fecha_inicio %>
  </div>

  <div class="field">
    <%= f.label :fecha_final %>
    <%= f.datetime_select :fecha_final %>
  </div>

    <div style="margin-bottom: 20px" class="input-group">
      <%= f.label "Area of Expertise" %><br />
      <%= f.collection_check_boxes :usuario_proyectos, Usuario.all, :id, :nombre do |b| %>
          <div class="collection-check-box">
            <%= b.check_box %>
            <%= b.label %>
          </div>
      <% end %>
    </div>

  <div class="field">
    <%= f.submit %>
  </div>

<% end %>